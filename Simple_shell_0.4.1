#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(int argc, char *argv[]) {
    if (argc != 2) {
        fprintf(stderr, "Usage: %s status\n", argv[0]);
        exit(1);
    }

    int status = atoi(argv[1]);
    exit(status);
}

